# Handoff — OD-RECOVERY-038 (2026-08-03)

## Sessions worked

- `OD-RECOVERY-038` (4 attempts; see ledger) — **Partial**
- Durable results: **lobby-login hypothesis ruled out** (corrects OD-037) and
  **401-refresh hardening validated live** (first failure in 13 validations →
  fixed with 750ms settle + 4 retries).

## Result statuses

- **OD-RECOVERY-038: Partial.** The OD-037 "new lobby-login crash signature"
  is **ruled out**: the `Invalid password status=68` login failure appears in
  *every* blitz log since 14:48 game-time — *including the OD-036 SUCCESS
  run* (15:03 log: login failure + full replay to `onLeaveWorld`) — and every
  launch reaches `Start replay event` + `LoadGameScene`. The real death
  signature is `become hidden` + `GameCore::OnBackground` ~1–2s after
  `LoadGameScene` ends (quiet game-side exit, no crash dump, no assert) — the
  known replay-start flake family at an elevated rate (7 of last 8 before
  this session; 2 of 4 this session).
- **First-ever 401-refresh failure** (OD-038 attempt 3, round 9; the mechanism
  survived 13 prior validations OD-030…036). Root cause: the refreshed context
  re-read the rendezvous file but retried immediately into a mid-rotation
  file, exhausting the 2-retry budget. **Fixed** in
  `roll-replay-time-increased.ps1`: `Retries` 2→4 + `Start-Sleep -Milliseconds
  750` after each refresh. **Validated live on first use** — attempt 4
  round-8 401 absorbed, roll continued.

## Key evidence

- **Login failure is baseline noise:** grep across all blitz logs shows
  `LoginHandler::fail status=68 Invalid password` in *every* log from
  `blitz-logs_20260803144823` onward, including the successful OD-036-era
  `blitz-logs_20260803150349` (login failure + `Start replay event` +
  `onLeaveWorld` full replay). The failure began at ~14:48 game-time (a
  stored-token rejection) and never blocked the replay path.
- **Real death signature:** `Window::HandleVisibilityChanged: become hidden` +
  `GameCore::OnBackground` ~1–2s after `BattleController::LoadGameScene ends`,
  then the log stops. No `CrashDumps` entry today (dumps are all 08-02), no
  assert line — a quiet exit at replay start.
- **Attempt 3 roll:** gate green, `39,190,222 → … → 65` in 9 rounds (budget
  bound), then round-9 401 → `FAILED_unexpected` — the first 401-refresh
  failure. Gate still `OfflineReplayVerified` after.
- **Attempt 4 roll (hardened):** gate green, round-8 401 absorbed
  (`capability_401_refresh_retry=1`, roll continued), `39,126,523 → … → 11`
  in 20 rounds, plateau **11** rounds 16–20 (1s pulses — value-bound tail),
  lease wall round 21 (`400` + `EvidenceStale`). No target (11 > 10), no
  address file, no CE staging.

## For the next session (OD-039)

1. The pipeline is at best-known state; the 401-refresh hardening is in place.
2. Run the proven invocation
   `-SnapshotMaxBytes 402653184 -MaxRounds 40 -HoldAfterRollSeconds 240` with
   **the operator present** during the held green window. The 11-survivor set
   is usable for interactive Find-what-writes even without ≤10.
3. The binding constraint is the **replay-start flake** (quiet exit ~2s after
   `LoadGameScene`) at an elevated rate — expect relaunch, not re-click.
4. BLK-0019 still needs a content-distinct second replay (`independentReplays`
   still 0).

## Artifacts

- `docs/operations/offset-discovery-ledger.md` — OD-038 record + corrected
  decision register
- `docs/operations/offset-discovery-workflow.md` — OD-039 protocol + rules
  (12/13)
- `scripts/roll-replay-time-increased.ps1` — 401-refresh hardening (settle +
  retries)
- Raw: blitz logs `20260803154751/155628/155812.txt`, session logs
  `/tmp/od-launch-038{,b,c,d}.log`, `%TEMP%\od-ce-autorun.log`
