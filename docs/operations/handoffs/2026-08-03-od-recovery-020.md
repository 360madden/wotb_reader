# Session handoff — 2026-08-03: OD-RECOVERY-020 (autorun write-BP probe campaign)

**Author:** Codex Agent

**Branch:** `main` (head `880b6f3`; working tree: OD-018 + OD-019 recording
still uncommitted, plus OD-020 recording and untracked temp helpers
`scripts/od-query-state.ps1` / `scripts/od-018-session.ps1`, and new
`tools/cheat-engine/od-autorun-writebp.lua`)

## Outcome

`OD-RECOVERY-020` ran as **Partial** — a three-live-run probe campaign
(020/021/022) that tested a fully automated replacement for the operator-owned
Find-what-writes step:

- **CE autorun mechanism live-proven.** CE 7.7 executes `.lua` files in its
  `autorun` folder at startup. The new `tools/cheat-engine/od-autorun-writebp.lua`
  (installed to `C:\Program Files\Cheat Engine\autorun\`) attaches to
  wotblitz, stages the rolling survivors into CE's address list, and arms
  hardware write breakpoints via `debug_setBreakpoint` — all live-proven.
- **`debug_setBreakpoint` is the correct CE 7.7 arming API** (resolved at
  runtime; `setBreakpoint` is nil). 4 breakpoints armed = the x64 DR0–DR3
  hardware limit.
- **Zero breakpoint hits captured** across all three runs, despite replayTime
  advancing every frame during the 20s capture window — matching the
  OD-009/010/011 zero-hit history. The automated Windows-debugger write-BP
  path is **ruled out**; the operator-owned interactive step is required, not
  a scripting gap.
- Rolling still improved: **3084237→…→5** (5 survivors, tightest ever).

## Session record

Session ID: `OD-RECOVERY-020` (probe campaign over live runs 020/021/022).
Status: **Partial**. Prior: OD-019 (Partial). BLK-0019 remains open — only the
single Churchill replay (sha12 `0FAE5612491E`) exists in the game folder;
`independentReplays` still 0.

### Run log

| Run | Rolling sequence | Autorun outcome | Hits |
|---|---|---|---|
| 020 | `2749700→…→6` | attached, staged 6, `setBreakpoint` nil → armed 0 | 0 |
| 021 | `2085299→…→8` | attached, staged 8, `debug_setBreakpoint` resolved, armed 4/8 (HW limit), `waitForBreakpoint` invalid | 0 |
| 022 | `3084237→…→5` | attached, staged 5, armed 4 (1 skipped), `onBreakpoint` callback + wait fallback, 20s live window | 0 |

### What was tried and why it failed

1. **CE CLI Lua (`-luac`/`--luac`)** — marker-file probe: both rejected on CE
   7.7. No CLI auto-execution.
2. **CE autorun folder** — works. Script attached, staged, and armed BPs.
3. **Breakpoint arming API** — `setBreakpoint` is nil in CE 7.7;
   `debug_setBreakpoint(address, bptWrite, 1)` resolves and arms (4 = HW cap).
4. **Hit capture** — `waitForBreakpoint` is not a valid CE Lua function; the
   global `onBreakpoint` callback was defined and a wait-loop fallback probed
   multiple candidate names; neither produced a hit in a 20s window during
   active playback. Mechanism cause unknown (debugger may not be actually
   breaking, or the write source is not traceable via this path).

## Rules out / do not repeat

- **Ruled out:** CE CLI Lua auto-execution (`-luac`/`--luac`) as an automation
  path; automated CE Windows-debugger write-BPs on rolling Double survivors as
  a replacement for interactive Find-what-writes (zero hits across
  OD-009/010/011 and OD-020/021/022).
- **Do not repeat without a changed hypothesis:** further probing of the
  automated CE write-BP capture path — try a changed mechanism (e.g. x64dbg,
  installed at `C:\work\tools\x64dbg`) or the operator-present interactive
  step instead.

## Validation commands run

- `dotnet build WotBTreader.sln -c Release` — 0 warnings/0 errors.
- Live gate queries — `OfflineReplayVerified` each run; `Denied /
  evidence.monitor_unhealthy` after lease expiry (expected).
- CE autorun logs (`%TEMP%\od-ce-autorun.log`) reviewed after each run;
  `%TEMP%\od-ce-hits.log` empty across all three.
- Lua structural balance check (no interpreter on machine) — balanced,
  max depth 5, via `tmpwotb-e2e/lua_balance.py`.
- Post-review fixes re-validated: `os.exit` removed from the inert gate
  (main() wrapper with returns — avoids killing CE on normal launches),
  address-file freshness now enforced inside the session driver (deleted
  before rolling so the autorun never stages stale addresses), and
  `readRip()` numeric-guarded (no `string.format("%X", table)`). Lua
  balance re-checked and `scripts/od-018-session.ps1` re-parsed: both OK.

## Changed files this session

- `tools/cheat-engine/od-autorun-writebp.lua` — **new** autorun capture script
  (committed candidate; inert outside OD sessions via pre-arm marker gate).
- Installed copy at `C:\Program Files\Cheat Engine\autorun\od-autorun-writebp.lua`
  (machine-local, untracked; inert without the OD pre-arm marker).
- `docs/operations/offset-discovery-ledger.md` — OD-020 index row + full YAML
  record; decision register `Next planned session` → OD-023; header updated.
- `docs/operations/offset-discovery-workflow.md` — next-session protocol →
  OD-023; autorun probe conclusion.
- `docs/operations/handoffs/2026-08-03-od-recovery-020.md` — this handoff.
- `tmpwotb-e2e/lua_balance.py` — untracked Lua structure checker.
- Prior OD-018/019 recording remains uncommitted in the working tree.

## Assumptions and unknowns

- Why the armed write BPs never fired is not root-caused (debugger not
  actually breaking vs untraceable write source). Recorded as a rules-out, not
  an open automation gap to keep probing.
- The 5-survivor address file (`%TEMP%\od-survivors.txt`) remains staged for a
  future operator-present interactive session.
- The autorun script stays installed (inert outside OD sessions); remove it
  from the CE autorun folder if it ever misbehaves in normal CE use.
- BLK-0019 (content-distinct second replay) still open.

## Integration risks

- The interactive step must run while the gate is green — the operator should
  use the staged address file immediately post-verify in the next session.
- Detached launch + session driver remains the proven launch pattern; avoid a
  blocking foreground helper.

## Recommended next steps

1. Run `OD-RECOVERY-023` (next session; 120s research-lease timebox) with the
   **operator present**: detached launch → session driver → rolling to ≤10 →
   **operator interactive Find-what-writes immediately post-verify** on the
   staged survivors (CE pre-armed; address file ready). Consider x64dbg
   (installed) for instruction/register evidence.
2. Commit the OD-018/019/020 recording + the autorun script + session driver
   when asked.
3. Place a content-distinct second `.wotbreplay` in the game folder for
   BLK-0019 when available.
4. Push `03ff3a4`, `e9b2bd9`, `6be2f64`, `63845d7`, `880b6f3` (+ recording
   units) to origin/main when asked.
