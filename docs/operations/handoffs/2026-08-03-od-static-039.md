# Handoff — OD-039-STATIC: Track A batch static root analysis (2026-08-03)

## Sessions worked

- `OD-039-STATIC` (Track A batch static analysis; no game process, no lease).
- Continuing the strategy-v2 workflow: batch scans, delta driver wiring,
  documentation propagation.

## What shipped

| Artifact | Purpose | Status |
|---|---|---|
| `tools/find-static-roots.py` | Batch `--rtti`/`--chain` args (comma-separated class/candidate lists) | New, run-verified |
| `scripts/roll-replay-time-increased.ps1` | `-CompareMode delta` / `-DeltaTarget` / `-DeltaTolerance` pass-through | Implemented, parse-checked |
| `docs/operations/offset-discovery-strategy-v2.md` | §4/§8 updated with batch-scan evidence + driver pilot ready | Updated |
| `docs/operations/offset-discovery-workflow.md` | Header + next-session (OD-040) updated with static milestone | Updated |
| `docs/operations/offset-discovery-ledger.md` | OD-039-STATIC row + full result section + decision-register update | Updated |

## Evidence produced (static, no game process)

Batch run over the hash-bound 11.19.0.10 binary (9 RTTI class scans + 3 chain
verifications):

- **Two runtime-written static root candidates confirmed:**
  - `0x03FA0C74` — `.data`, zero-on-disk, **9 .text operand references**
    (samples `0x0005D531`, `0x006E52D1`, `0x006E64D1`, `0x006F18C1`, …).
  - `0x03FA012C` — `.data`, zero-on-disk, **6 .text references** (samples
    `0x005F7BBB`, `0x005F7BCE`, `0x00601764`, …).
  - Both **fail the reloc test** — the code-initialized-global signature (a
    reloc target would be a compiler-generated pointer; a non-reloc slot with
    .text operands is written by code at init). Highest-confidence static
    roots found to date.
- **`EntityList` is a plain struct**: 0 RTTI hits → no vtable/COL anchor;
  xref discovery is the only path (the 2,398-store-slot scan already covers it).
- **RTTI TypeDescriptors located for the whole chain family**: VehicleGameLogic
  family (11 mangled), AppContextImpl (3), ScreensFlow (1), GameScene (5),
  GameCamera* (6), VehicleDescr (20), Vehicle*Component (809 mangled hits),
  Context (244) — Ghidra anchors for later analysis.
- **No statically-reachable vtable root**: all `Vehicle` (8) and `Context` (7)
  COL slots fail the signature check (`plausible=False`) — consistent with the
  strategy-v2 pilot's `AvatarContextBattle` no-root result.
- `0x03E7DF28` (AvatarContextBattle td from the strategy-v2 pilot) verified as
  dead data: **0 .text references** → cannot anchor a chain.

## Rules-out (repeat only with a changed hypothesis)

- Statically-reachable vtable roots for the chain classes as a Track A
  mechanism (COL signature mismatch across all 15 slots examined).

## Blockers / open items

- BLK-0019 still needs a content-distinct second replay (`independentReplays` 0).
- No static root is live-confirmed; `0x03FA0C74`/`0x03FA012C` need a live
  replayTime-anchored verification (Track C2 step 4 in strategy-v2).
- The replay-start flake (quiet exit ~2s after `LoadGameScene`, no dump)
  remains the binding live-session constraint.

## Next session (OD-RECOVERY-040)

1. Run the proven invocation `-SnapshotMaxBytes 402653184 -MaxRounds 40
   -HoldAfterRollSeconds 240` with the operator present during the held green
   window; the 11-survivor set is usable for interactive Find-what-writes even
   without ≤10.
2. Optionally pilot **delta-compare** (`-CompareMode delta -DeltaTarget
   <replay position delta> -DeltaTolerance <tol>`) to break the value-bound
   11-survivor plateau.
3. If the window allows, verify `0x03FA0C74`/`0x03FA012C` live.
4. Content-distinct second replay when available (BLK-0019).

## Validation performed

- `python -m py_compile tools/find-static-roots.py` — OK; `--help` shows the
  new batch args.
- PowerShell parser check on `roll-replay-time-increased.ps1` —
  `DRIVER_PARSE_OK`.
- `offline_check.py` — 22 files, 85 links, 0 broken; BLK-0001..BLK-0025
  contiguous; ledger consistency OK (25 result sections, 39 index rows).

## Unrelated local/untracked material left untouched

- Pre-existing working-tree modifications to
  `docs/operations/handoffs/2026-08-02-od-recovery-014-partial.md` (not part of
  this change).
- `.freebuff/worktrees/` untracked directory (not part of this change).
