# Handoff: OD-RECOVERY-024 (operator window held in-command)

**Date:** 2026-08-03
**Status:** Partial — pipeline delivered, window held green, interactive step still operator-owned
**Campaign:** WoT Blitz PC offset discovery (`playerYaw` hypothesis quarantined; offset remains 0)

## Outcome

The proven pipeline ran end-to-end for the seventh consecutive time, and this
session closed the lease-margin gap from a different angle: the session driver
now holds the operator window **inside the running command**
(`-HoldAfterRollSeconds 60`), printing the Find-what-writes instruction inline
and polling the gate for the full hold.

| Step | Result |
|---|---|
| Launch (detached) | Gate `OfflineReplayVerified` (`watch_exit=0`, dialog dismissed) |
| Pre-arm | CE 7.7 attached to game PID 8428 |
| Autorun staging | All 8 survivors staged in CE address list as `od-survivor-1..8`; `debug_setBreakpoint` armed 4 (x64 HW limit), 0 hits |
| Rolling (fixed driver) | `767529→…→8` — 8 survivors, address file exactly 8 lines, no WARN |
| **Operator hold (60s)** | **Gate stayed `OfflineReplayVerified` the entire hold — window closed by its own timer, not gate loss (first time)** |

## Session record

- **Ledger:** OD-RECOVERY-024 index row + full YAML record; decision register → OD-RECOVERY-025; header updated.
- **Workflow doc:** next-session protocol → OD-RECOVERY-025 with the in-command hold note.
- **Handoff:** this file.

## What was done

1. Pre-flight: stale OD-023 processes (game 43460, CE 41312) stopped by the launch helper; autorun script confirmed installed.
2. Session driver: added `-HoldAfterRollSeconds` operator-window hold (announce instruction + inline gate polling after rolling lands ≤10). Also fixed a PS 5.1 encoding hazard (UTF-8 em-dash in a BOM-less .ps1 broke string token parsing — replaced with plain hyphen).
3. Release build: 0 warnings / 0 errors.
4. OD-024 live run: detached launch helper → session driver with `-HoldAfterRollSeconds 60`.
5. Rolling `767529→…→8`; autorun staged 8 survivors, armed 4 write-BPs (0 hits — consistent with the OD-020 rules-out).
6. Gate polled `OfflineReplayVerified` through the full 60s hold; window closed by timer expiry, not gate loss.
7. Recorded per repo rules (ledger + workflow + this handoff).

## Rules out / do not repeat

- **Do not repeat:** automated CE Windows-debugger write-BP capture as a
  replacement for interactive Find-what-writes (OD-020 rules-out; zero hits
  again this session). The interactive right-click remains the evidence path.
- CE CLI `-luac`/`--luac` Lua auto-execution remains unsupported (OD-019
  negative; no change).

## Assumptions and unknowns

- Rolling intermediates for OD-024 were printed to the live terminal only and
  not persisted; the ledger records head/final (`767529→…→8`) per the
  evidence-first rule.
- Same Churchill replay sha12 `0FAE5612491E`; `independentReplays` still 0
  (BLK-0019 open — content-distinct second replay still required).
- No RIP/root-address evidence recorded yet; the offset remains 0.

## Integration risks / operational notes

- The operator hold means the next session's driver will block for the full
  hold duration while the gate is green — the operator should be at the
  machine, or the hold should be shortened (e.g. `-HoldAfterRollSeconds 30`).
- Stale CE / host processes are stopped automatically by the next launch helper.

## Validation commands

```powershell
dotnet build WotBTreader.sln -c Release   # ran this session: 0 warnings / 0 errors
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/od-018-session.ps1 -ParseCheckOnly   # ran this session: PARSE_OK
./scripts/validate.ps1   # full gate — run before milestone commits (not run this session)
```

## Changed files this session

- `docs/operations/offset-discovery-ledger.md`
- `docs/operations/offset-discovery-workflow.md`
- `docs/operations/handoffs/2026-08-03-od-recovery-024.md`
- `scripts/od-018-session.ps1` (temp helper; `-HoldAfterRollSeconds` operator hold; encoding fix)

## Recommended next steps

1. **OD-RECOVERY-025 (120s research-lease timebox):** run the pipeline with the
   operator present; during the 60s in-command hold, right-click any
   `od-survivor-N` entry in CE → "Find out what writes this address" and let
   the replay play. Record the write instruction + register (member
   displacement like `[reg+0x34]`).
2. Import a content-distinct second `.wotbreplay` to close BLK-0019
   (`independentReplays`).
