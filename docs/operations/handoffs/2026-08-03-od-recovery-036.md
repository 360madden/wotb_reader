# Handoff — OD-RECOVERY-036 (2026-08-03)

## Sessions worked

- `OD-RECOVERY-036` (4 attempts; see ledger) — **Partial**

## Result statuses

- **OD-RECOVERY-036: Partial — full staging handoff proven end-to-end.**
  Attempt 4 reached **TARGET 10 ≤ 10 in 17 rounds** with the budget driver
  (round-1 walked 39M), harvested **9 candidates**, and the CE autorun
  **staged all 9 into CE's address list with 4 hardware write-BPs armed**
  (0 hits in a 20s capture, consistent with the OD-020 standing rule).
  The operator window opened but the gate was `EvidenceStale` at close (the
  120s lease expired during the 240s hold) — the interactive Find-what-writes
  result remains operator-owned. Offset remains 0; nothing promoted.

## Successes

1. **TARGET ≤10 with the proven invocation** (`-SnapshotMaxBytes 402653184
   -MaxRounds 40`): sequence `772551→161108→34755→6524→2363→710→173→84→46→
   16→15→13→13→11→11→11→10`, `rolling_exit=0`.
2. **Full CE staging handoff proven end-to-end**: `loaded 9 survivor lines →
   staged 9 in address list → debugger attached → armed 4 HW write-BPs → 20s
   capture, 0 hits`.
3. **401 refresh fired live an eleventh time** (round 6).
4. The budget + tail-shave combination converges to ≤10 reliably now.

## Failures / open items

- **The operator window did not stay green**: the 120s lease expired during
  the 240s hold, so no live interactive Find-what-writes result — the staged
  set (9 addresses, `%TEMP%\od-survivors.txt`) is ready for the operator.
- **3 of 4 launches hit the game-side flake** this session (attempt 1 died in
  soft-focus settle; attempts 2/3 flipped to `Denied`/`monitor_unhealthy`
  before the driver's first poll) — an unusually high rate; budget relaunches.
- **BLK-0019**: still needs a content-distinct second replay
  (`independentReplays` 0).

## Explicitly ruled out

- No new rules-outs. Automated CE write-BPs on rolling Double survivors stay
  ruled out (OD-020; 0 hits again).

## Must not repeat without a changed hypothesis

- Same standing rules (default CE address-file path; 300s autorun poll;
  budget + tail shave + `MaxRounds` passthrough). No new do-not-repeat items
  this session.

## Next session ID and timebox

- **OD-RECOVERY-037**: the staged set + armed BPs are the deliverable; the
  operator-window interactive Find-what-writes step is the remaining evidence
  path. Run `-SnapshotMaxBytes 402653184 -MaxRounds 40 -HoldAfterRollSeconds
  240` with the operator present during the held green window; alternatively a
  content-distinct second replay for BLK-0019.

## Validation commands and results

- Live gate state at session end: `EvidenceStale` / `evidence.expired`
  (lease expiry, expected teardown state).
- CE autorun log: staged 9, armed 4, capture `hits=0`.
- Process teardown verified clean (no wotblitz / cheatengine / Host.Web /
  dotnet leftovers).

## Unrelated local/untracked material left untouched

- `docs/operations/handoffs/2026-08-02-od-recovery-014-partial.md` remains a
  line-ending-only artifact (blob matches index; no real change).
