# Handoff — OD-RECOVERY-032 (2026-08-03)

## Sessions worked

- `OD-RECOVERY-032` (2 attempts; see ledger) — **Partial**

## Result statuses

- **OD-RECOVERY-032: Partial.** Two attempts, both converged to **11
  survivors** (one short of the ≤10 target) inside the 120s lease, then the
  lease expired 1–2 rounds later (round-15 and round-13 `400` discard
  signatures + `EvidenceStale`). The **11-survivor plateau is the
  reproducible ceiling** on this machine's load. No address file staged, no
  operator window. Offset remains 0; nothing promoted.

## Successes

1. **Pipeline reproduces cleanly** — the driver at best-known state (401
   refresh + folded gate + candidate-count harvest + default-path CE staging)
   reached 11 survivors in 12–14 rounds on both attempts.
2. **401 refresh fired live a sixth time** (attempt 2 round 8, attempt 1
   round 10) — long rolls keep surviving token rotation.
3. **Attempt 1's high `retained=177581` carryover** (unreadable chunks) was
   observed and confirmed harmless: `increasedCount` remains the survivor
   count and the compare still narrows correctly.

## Failures / open items

- **Tail sits at the lease edge**: 11 → ≤10 needs 1–2 more rounds than the
  120s lease allows on this machine's load. The roll fits the lease; the last
  rounds do not.
- No address file, no CE staging, no operator window (target not reached).
- **BLK-0019**: still needs a content-distinct second replay
  (`independentReplays` 0).

## Explicitly ruled out

- No new rules-outs. Automated CE write-BPs on rolling Double survivors stay
  ruled out (OD-020); the interactive operator step remains the evidence
  path.

## Must not repeat without a changed hypothesis

- Same rules as OD-031 (default CE address-file path; 300s autorun poll; 1
  candidate per rolling round with harvest on target only). No new
  do-not-repeat items added this session.

## Next session ID and timebox

- **OD-RECOVERY-033**: same pipeline; the tail needs lease headroom, so
  prioritize (a) reducing round-1 walk cost (snapshot budget / region
  selection), (b) shaving tail transition time, or (c) running in a
  lighter-loaded window. A content-distinct second replay still helps
  BLK-0019.

## Validation commands and results

- Live gate state at session end: `EvidenceStale` / `evidence.expired`
  (lease expiry, expected teardown state).
- CE autorun polled 300s both attempts; no address file appeared (target not
  reached — correct behavior).
- Process teardown verified clean (no wotblitz / cheatengine / Host.Web /
  dotnet leftovers).

## Unrelated local/untracked material left untouched

- `docs/operations/handoffs/2026-08-02-od-recovery-014-partial.md` remains a
  line-ending-only artifact (blob matches index; no real change).
