# Handoff — OD-RECOVERY-037 (2026-08-03)

## Sessions worked

- `OD-RECOVERY-037` (4 attempts; see ledger) — **Partial**

## Result statuses

- **OD-RECOVERY-037: Partial.** Launch reliability is now the binding
  constraint, not the roll. All 4 attempts flaked game-side before a roll
  could start (7 of the last 8 launches across OD-036/037). A **NEW crash
  signature** appeared in the blitz logs: the game never reaches the replay —
  it dies in the login/lobby phase (`LoginHandler::fail status=68 Invalid
  password` + `ConnectionManager::onLogOnFailure`, then
  `Window::HandleVisibilityChanged: become hidden` + `GameCore::OnBackground`),
  distinct from the OD-030/031 replay-start AccountController assert. This
  may indicate an online-login requirement blocking the offline path. Offset
  remains 0; nothing promoted.

## Successes

1. **NEW lobby-login crash signature characterized**: the game dies in the
   login/lobby phase (`Invalid password` status=68) with the window hidden
   before the replay — distinct from the replay-start assert seen before.
2. **The pipeline behaved as designed on all four attempts**: launcher,
   clicker, and gate revocation (`evidence.monitor_unhealthy`) all correct.
3. The roll pipeline remains proven (OD-036: TARGET 10 ≤ 10, 9 survivors
   staged in CE, 4 HW write-BPs armed) — no driver change is warranted.

## Failures / open items

- **No roll ran**: all four launches died before the session driver's gate
  poll returned green.
- **Elevated flake rate**: 7 of the last 8 launches across OD-036/037.
- **BLK-0019**: still needs a content-distinct second replay
  (`independentReplays` 0).

## Explicitly ruled out

- No new rules-outs. The staging handoff and rolling-to-≤10 remain proven;
  the blocker is launch reliability.

## Must not repeat without a changed hypothesis

- Re-running the proven invocation without first diagnosing the lobby-login
  failure (OD-037: 4-of-4 flakes). OD-038 must check the offline flag / login
  config / network path first.

## Next session ID and timebox

- **OD-RECOVERY-038**: first diagnose the lobby-login failure (offline flag /
  login config / network), then run the proven invocation
  (`-SnapshotMaxBytes 402653184 -MaxRounds 40 -HoldAfterRollSeconds 240`)
  with the operator present during the held green window; a content-distinct
  second replay still closes BLK-0019.

## Validation commands and results

- Live gate state at session end: `Denied` / `evidence.monitor_unhealthy`
  (game dead — expected after the flake).
- Blitz logs `blitz-logs_20260803152454/152658/152855.txt` captured the
  lobby-login failure signature.
- Process teardown verified clean (no wotblitz / cheatengine / Host.Web /
  dotnet leftovers).

## Unrelated local/untracked material left untouched

- `docs/operations/handoffs/2026-08-02-od-recovery-014-partial.md` remains a
  line-ending-only artifact (blob matches index; no real change).
