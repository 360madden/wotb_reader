# Handoff — OD-RECOVERY-039 (2026-08-03)

## Sessions worked

- `OD-RECOVERY-039` (5 attempts; see ledger) — **Partial**
- Durable results: **full-lease green roll proven** (first time the proven
  invocation ran its complete 40-round budget inside the 120s research lease
  with the game surviving the entire evidence lifetime) and the
  **plateau-staging gap fixed** (`-HarvestThreshold` driver param).

## Result statuses

- **OD-RECOVERY-039: Partial.** Attempts 1–4 reproduced the elevated
  replay-start flake: the launcher reached `OfflineReplayVerified`
  (`watch_exit=0`, round-2 click) then the game quietly exited ~2s after
  `LoadGameScene` ends (`become hidden` + `GameCore::OnBackground`, no crash
  dump) — 4 of 5 attempts this session. Attempt 5 (chain attempt 2) was the
  **first full-lease green roll since OD-031**: gate verified 22:54:57Z, roll
  `2,090,084 → 50,112 → 10,364 → 2,512 → 239 → 66 → 48 → 32 → 29 → 26 → 23 →
  18 → 17 → 15 → 14 → 13` in 40 rounds (`rolling_exit=0`), gate held
  `OfflineReplayVerified` through the entire 120s lease and closed only at
  evidence expiry 22:56:56Z — closed by the lease wall, not a flake.
- The durable finding: **the converged plateau set is never staged** because
  the driver writes the address file only when survivors drop to/below the
  ≤10 target. This run wrote 1 candidate (`0x74A2478`) with
  `WARN_address_count_mismatch candidates=1 survivors=13`, so the operator
  window had nothing to Find-what-writes on. **Fixed** in
  `roll-replay-time-increased.ps1` with a new `-HarvestThreshold` param
  (final 500-candidate compare stages the full converged tail before discard,
  only while the gate is still verified); passthrough added to
  `od-018-session.ps1`.

## Key evidence

- **Blitz-log death signature (attempts 1–4):** `Start replay event` →
  `LoadGameScene begins` → `LoadGameScene ends` → ~2s later `become hidden` +
  `GameCore::OnBackground`, log stops. No `CrashDumps`, no assert.
- **Attempt 5 roll:** round-1 `previous=2,090,084` (budget-bound; smaller
  than OD-038's 39.1M — footprint variance), full 40 rounds, plateau **13**
  (within the OD-032/033 11–17 convergence range), `rolling_exit=0`.
- **CE staging:** autorun attached pid 53100, staged 1 survivor, armed 1
  write-BP, 20s capture, **hits=0** — consistent with the OD-020
  automated-write-BP negative.
- **Expiry-termination observation:** the managed child (wotblitz 53100) was
  still alive ~7 min past evidence expiry (last observed 23:03Z), gate
  staying `EvidenceStale` (closed = no scanning authorized). Recorded as a
  dated amendment to BLK-0024; next launch helper force-stops stale
  processes regardless.

## For the next session (OD-040)

1. The pipeline is at best-known state and the plateau-staging gap is fixed.
2. Run the proven invocation WITH `-HarvestThreshold 200`
   (`-SnapshotMaxBytes 402653184 -MaxRounds 40 -HarvestThreshold 200
   -HoldAfterRollSeconds 240`) with **the operator present** during the held
   green window — the staged plateau set (11–17) is directly usable for
   interactive Find-what-writes even without ≤10.
3. Confirm the BLK-0024 expiry-termination observation: does
   `RevokeSession(terminateProcess: true)` → `TryTerminateAfterHandOff` fire
   on the evidence-expiry path, and why did the child survive?
4. The binding constraint remains the replay-start flake (4 of 5 this
   session) — expect relaunch, not re-click.
5. BLK-0019 still needs a content-distinct second replay
   (`independentReplays` still 0).

## Artifacts

- `docs/operations/offset-discovery-ledger.md` — OD-039 record + index row +
  decision register (next planned `OD-RECOVERY-040`)
- `docs/operations/offset-discovery-workflow.md` — OD-040 protocol + rule (14)
- `docs/operations/blocker-log.md` — BLK-0024 dated amendment
  (expiry-termination observation)
- `scripts/roll-replay-time-increased.ps1` — `-HarvestThreshold` tail staging
- `scripts/od-018-session.ps1` — `-HarvestThreshold` passthrough
- Raw: blitz-logs_20260803174515.txt, session logs
  `/tmp/od-launch-039{,b,c,d}.log`, `/tmp/od-session-039.log`,
  `/tmp/od-roll-039.log`, `%TEMP%\od-ce-autorun.log`,
  `%TEMP%\od-survivors.txt`, `%TEMP%\od-prearmed-debugger.json`
