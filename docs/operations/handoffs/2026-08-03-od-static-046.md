# Handoff — OD-046-STATIC (2026-08-03)

## Sessions worked

- `OD-046-STATIC` — movement-only windows + HP damage-delta markers for the
  live Track C2 pilot. **Partial** (static evidence only; no live lease spent).

## Result statuses

- **OD-046-STATIC: Partial.** `replay-delta-extractor.py --movement`
  segmented the 11.19.0 medvedkovo replay into moving/stationary phases:
  **only 32.3% of 1s windows are moving (891/2,756 @ 0.5 m/s)**, with
  moving-window displacement median 0.712 m, p90 0.992, max 1.489 — the
  Float position pilot should scan a movement-only span so the position
  marker is actually selective (fixes the OD-045 hollow-collapse finding in
  practice). `--hp-delta --victim-entity <id>` decoded kind-3 damage events
  into a per-window damage series and ran it through the survival sim: the
  player (entity 2549401) took **0 damage** this replay (marker conditional
  on a damaged victim); a hit victim (2549395) showed sparse-but-exact
  signals (5 hit windows, 2,618 dmg, pass-rate 0.9808 @ tol 0, survival
  0.907/0.824/0.747 over 5/10/15 rounds) — a supporting marker, never the
  primary filter.
- **Fix shipped:** the in-flight OD-046 edit had truncated the module
  docstring closer (line 47 was `""` instead of `"""`), breaking both new
  modes (`SyntaxError invalid decimal literal` at line 144 + `TokenError
  EOF-in-string` at 374). Root-caused via tokenize, fixed, both modes proven
  end-to-end.

## Blockers / next steps

- **BLK-0019** still needs a content-distinct second replay
  (`independentReplays` still 0) before any offset promotion.
- **OD-RECOVERY-044 (live, operator present)** remains the binding next
  milestone: run the **Double replayTime delta pilot FIRST**
  (`-ValueKind Double -CompareMode delta -DeltaTarget 4.0 -DeltaTolerance
  0.4`, unit variants 4000ms / 4,000,000ticks), then the Float position
  pilot on a movement-only span, then operator Find-what-writes on the
  staged set.
- Ranked live pilot markers (all statically validated): 1) Double replayTime
  delta (deterministic, OD-045), 2) Float position delta on a movement-only
  span (OD-046 movement fraction now quantified), 3) HP damage-delta as
  supporting evidence if a damaged victim is available (OD-046).

## Files touched

- `scripts/python/replay-delta-extractor.py` — added `--movement`,
  `--speed-threshold`, `--hp-delta`, `--victim-entity` modes; fixed
  truncated module-docstring closer.
- `docs/operations/offset-discovery-ledger.md` — OD-046-STATIC header, index
  row, full result section.
- `docs/operations/offset-discovery-workflow.md` — next-session block:
  movement-only span + HP-delta conditional note.
- `docs/operations/offset-discovery-strategy-v2.md` — C2 section: movement +
  HP marker evidence.

## Validation

- `py_compile` clean; both new modes run exit 0 against session
  `019fb86c-c8e7-7004-9df6-a574f5a7835b`.
- `offset_check.py --check-schema` PASS.
- `offline_check.py` clean: 22 files / 85 links / 0 broken; ledger
  consistent (25 result sections / 39 index rows).
