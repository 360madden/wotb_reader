# Session handoff — 2026-08-03: OD-RECOVERY-018 (full automated pipeline to ≤10)

**Author:** Codex Agent

**Branch:** `main` (head `880b6f3`; working tree: docs updates for OD-018
recording, plus untracked temp helpers `scripts/od-query-state.ps1` and
`scripts/od-018-session.ps1` from this session)

## Outcome

`OD-RECOVERY-018` ran as **Partial** — but the **full automated pipeline
completed end-to-end for the first time**:

- Gate `OfflineReplayVerified` with the fixed clicker (`watch_exit=0`,
  `dialogGone=True`, `post_orange_pixels=0`).
- CE 7.7 pre-armed and attached to `wotblitz.exe` (PID 43432) during the green
  session.
- Fixed `increasedCount` rolling driver narrowed
  **743576→126761→23075→4660→726→90→33→25→24→18→17→12→8** over 13 rounds.
- `-AddressFile` staged exactly **8 candidate addresses** (count == survivors,
  no WARN) at `%TEMP%\od-survivors-018.txt`.
- The gate held `OfflineReplayVerified` through the entire rolling sequence —
  the OD-016/017 lease-margin objective is met.

The 120s research lease then expired before interactive Find-what-writes could
run on the staged survivors (operator-owned step). The game was terminated by
the lifecycle monitor at lease expiry (expected behavior).

## Session record

Session ID: `OD-RECOVERY-018`. Status: **Partial** (pipeline complete; root
step outstanding). Prior: OD-017 (Partial). BLK-0019 remains open — only the
single Churchill replay (sha12 `0FAE5612491E`) exists in the game folder;
`independentReplays` still 0.

### Attempt #1 — in-process helper stall (worked around)

- First invocation ran the launch helper in the foreground with a 420s wrapper
  timeout. The helper reached `watch_exit=0` (exit file written) but then
  stalled past 420s — the same signature as OD-017 attempt-1. The wrapper
  timeout killed the helper; gate flipped `EvidenceStale`.
- The stall is still not root-caused (suspected PrintWindow capture-freeze
  path). **Workaround:** relaunch the helper detached (`nohup`/background)
  and drive the session from a separate polling driver — this succeeded.

### Attempt #2 — detached launch + session driver (green)

- `scripts/launch-offline-replay-for-od.ps1` launched detached with output to
  `%TEMP%\od-018-2.out.log`; completed with `OK OfflineReplayVerified`.
- `scripts/od-018-session.ps1` (untracked, this session): polled the gate,
  then on `OfflineReplayVerified` ran `pre-arm-debugger.ps1 -AutoAttach`
  (found CE 7.7 + x64dbg; launched CE attached, exit 0) and immediately ran
  `roll-replay-time-increased.ps1 -TargetSurvivors 10 -TransitionSeconds 3
  -AddressFile "%TEMP%\od-survivors-018.txt"` (rolling_exit=0).
- Rolling used natural replay progression (no manual Space pulses; `-AutoSpace`
  not needed since the replay advances on its own after WATCH OFFLINE).
- Session discarded after rolling; gate still `OfflineReplayVerified` at that
  moment; lease expired shortly after and the monitor terminated the game.

## Rules out / do not repeat

- **Ruled out (no new beyond OD-017):** blob-ROI gate trust; `retainedCount`
  as the rolling survivor count.
- **Do not repeat without a changed hypothesis:** a blocking foreground
  launch-helper invocation under a short wrapper timeout — the post-watch
  helper stall past 420s wasted OD-018 attempt-1; the detached launch +
  polling session driver succeeded.

## Validation commands run

- `dotnet build WotBTreader.sln -c Release` — succeeded, 0 warnings/0 errors
  (fresh binaries for the `--no-build` host launch).
- Live gate queries via `scripts/od-query-state.ps1` — `OfflineReplayVerified`
  during the session; `EvidenceStale / evidence.expired` after lease expiry.
- Address file line count: 8 (matches survivors=8).
- `[System.Management.Automation.PSParser]::Tokenize` on both temp scripts:
  `scripts/od-query-state.ps1 PARSE_OK`, `scripts/od-018-session.ps1 PARSE_OK`.

## Changed files this session

- `docs/operations/offset-discovery-ledger.md` — OD-018 index row + full YAML
  record; decision register `Next planned session` → OD-019; header updated.
- `docs/operations/offset-discovery-workflow.md` — next-session protocol →
  OD-019; detached-launch operational note; `-TargetSurvivors` example name.
- `scripts/od-query-state.ps1` — **untracked** temp helper (gate state query).
- `scripts/od-018-session.ps1` — **untracked** temp session driver
  (wait-verified → pre-arm → roll). Candidate for commit as reusable tooling.
- `docs/operations/handoffs/2026-08-03-od-recovery-018.md` — this handoff.

## Assumptions and unknowns

- The in-process helper stall (post-watch hang past 420s) is worked around but
  not root-caused; detaching the launch helper avoids it.
- The 8 staged survivors have not been exercised by Find-what-writes; address
  kind remains `heap-dynamic`/`private-mapping` pending the interactive root.
- CE PID 43432 left attached to a dead game; the next launch helper stops
  stale CE, or the operator may close it.
- BLK-0019 (content-distinct second replay) still open.

## Integration risks

- Next launch should be detached + driver-driven (not a blocking foreground
  helper) to avoid the stall window.
- The 120s research lease remains the hard budget: interactive Find-what-writes
  must start immediately post-verify, using the already-staged address file
  (or the CE Lua pre-arm staging at `tools/cheat-engine/prearm-attach.lua`).

## Recommended next steps

1. Run `OD-RECOVERY-019` (next session; 120s research-lease timebox): detached
   `launch-offline-replay-for-od.ps1` → session driver (or Lua pre-arm) →
   fixed rolling to ≤10 with `-AddressFile` → **operator interactive
   Find-what-writes immediately post-verify** on the staged survivors (CE is
   pre-armed; lease margin reserved).
2. Commit the OD-018 recording (ledger + workflow + this handoff) when asked.
3. Consider committing the session driver as reusable tooling
   (`scripts/od-session-driver.ps1`).
4. Place a content-distinct second `.wotbreplay` in the game folder for
   BLK-0019 when available.
5. Push `03ff3a4`, `e9b2bd9`, `6be2f64`, `63845d7`, `880b6f3` (+ this unit) to
   origin/main when asked.
