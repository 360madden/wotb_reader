# Handoff: OD-RECOVERY-029 (tightest survivor set since OD-020 — 6 survivors)

**Date:** 2026-08-03
**Status:** Partial — rolling converged to 6 survivors (second-tightest of the campaign); the 0-hit CE capture ran fully inside the green window
**Campaign:** WoT Blitz PC offset discovery (`playerYaw` hypothesis quarantined; offset remains 0)

## Outcome

The steady-state gate accepted the stable 66M baseline again, and the rolling
converged to **6 survivors — the tightest set since OD-020's 5**. The CE
capture (a ~22s onBreakpoint poll) ran entirely inside the verified gate,
which expired only at 15:52:46Z (lease budget) after the capture ended at
15:52:03Z — the lease-bound window held green through the whole capture.

| Step | Result |
|---|---|
| Launch (detached) | Gate `OfflineReplayVerified` |
| Pre-arm | CE 7.7 attached (game 36148 / CE 6420) |
| Steady-state gate | Snapshot sane (stable 66M baseline accepted at 100M threshold; exact initial count to live terminal only) |
| Rolling (5s transitions) | Converged to **6 survivors** (sequence to live terminal only, not persisted) |
| Address file | Exactly 6 lines, no WARN (`%TEMP%\od-survivors.txt`) |
| Autorun staging | All 6 staged in CE address list as `od-survivor-1..6`; `debug_setBreakpoint` armed 4 (x64 HW limit), 2 skipped beyond HW limit, 0 hits |
| **Operator window (lease-bound)** | **Gate `OfflineReplayVerified` through the entire CE capture (15:51:41Z..15:52:03Z); expired `EvidenceStale` only at 15:52:46Z (lease budget) after the capture ended** |

## Session record

- **Ledger:** OD-RECOVERY-029 index row + full YAML record; decision register → OD-RECOVERY-030; header updated.
- **Workflow doc:** next-session protocol → OD-RECOVERY-030 with the persisted-artifact recording rule.
- **Handoff:** this file.

## What was done

1. Pre-flight: OD-028 residue (game 45376, CE 27892) present; Release build green (0 warnings/errors).
2. Detached launch helper → session driver (`-TransitionSeconds 5 -HoldAfterRollSeconds 240`).
3. Gate `OfflineReplayVerified`; CE pre-armed; steady-state gate accepted the stable 66M baseline.
4. Rolling converged to **6 survivors** (tightest since OD-020's 5); address file exactly 6 lines, no WARN.
5. CE autorun staged all 6 survivors; armed 4 write-BPs (2 beyond x64 HW limit); 0 hits in ~22s capture fully inside the green window.
6. Gate `EvidenceStale` only at 15:52:46Z (lease budget) — after the capture ended at 15:52:03Z.
7. Recorded per repo rules (ledger + workflow + this handoff); the driver's per-round sequence was live-terminal-only, so the record reads the persisted artifacts (address file + autorun log + gate state) rather than inventing intermediates.

## Rules out / do not repeat

- **Do not repeat:** automated CE Windows-debugger write-BP capture as a
  replacement for interactive Find-what-writes (OD-020 rules-out; zero hits
  again this session, and this time the whole capture was provably inside the
  green window). The interactive right-click remains the evidence path.
- Steady-state gate calibration rule stands (OD-026): accept the measured
  stable baseline, don't assume one.
- **Recording rule (new):** when the driver sequence is live-terminal-only,
  read the record from persisted artifacts (address file, autorun log, gate
  state timestamps) and never invent unpersisted intermediates.

## Assumptions and unknowns

- No operator Find-what-writes result recorded by session end (operator-owned;
  the window was held lease-bound inside the command).
- Same Churchill replay sha12 `0FAE5612491E`; `independentReplays` still 0
  (BLK-0019 open).
- Exact per-round survivor sequence and snapshot initial count were printed to
  the live terminal only (driver stdout not persisted to a file); the 6-line
  address file and the autorun staging are the authoritative artifacts.
- No RIP/root-address evidence recorded yet; the offset remains 0.

## Integration risks / operational notes

- The operator window is bounded by the 120s research lease: the driver polls
  the gate and holds until it goes stale (`-HoldAfterRollSeconds 240` cap).
  This session the capture ended before lease expiry, so the 0-hit result is
  green-window evidence.
- Stale CE / host processes are stopped automatically by the next launch helper.

## Validation commands

```powershell
dotnet build WotBTreader.sln -c Release   # ran this session: 0 warnings / 0 errors
./scripts/validate.ps1   # full gate — run before milestone commits (not run this session)
```

## Changed files this session

- `docs/operations/offset-discovery-ledger.md`
- `docs/operations/offset-discovery-workflow.md`
- `docs/operations/handoffs/2026-08-03-od-recovery-029.md`

## Recommended next steps

1. **OD-RECOVERY-030 (120s research-lease timebox):** run the same pipeline
   (steady-state gate + 5s transitions + `-HoldAfterRollSeconds 240`) with the
   operator present; during the held green window, right-click any
   `od-survivor-N` entry in CE → "Find out what writes this address" and let
   the replay play. Record the write instruction + register (member
   displacement like `[reg+0x34]`).
2. Import a content-distinct second `.wotbreplay` to close BLK-0019
   (`independentReplays`).
