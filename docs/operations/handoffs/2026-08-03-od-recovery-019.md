# Session handoff — 2026-08-03: OD-RECOVERY-019 (tightest survivor set; live interactive handoff)

**Author:** Codex Agent

**Branch:** `main` (head `880b6f3`; working tree: OD-018 recording still
uncommitted, plus OD-019 recording from this session and untracked temp
helpers `scripts/od-query-state.ps1` / `scripts/od-018-session.ps1`)

## Outcome

`OD-RECOVERY-019` ran as **Partial** — the full automated pipeline completed
end-to-end for a **second consecutive session**, with the tightest survivor
set yet:

- Gate `OfflineReplayVerified` (`watch_exit=0`, `dialogGone=True`).
- CE 7.7 pre-armed and attached to `wotblitz.exe` (PID 42388 → CE PID 45188).
- Fixed `increasedCount` rolling driver narrowed
  **2980777→73925→11283→2290→401→75→24→17→16→16→16→14→11→7** over 14 rounds.
- `-AddressFile` staged exactly **7 candidate addresses** (count == survivors,
  no WARN) at `%TEMP%\od-survivors-019.txt`.
- **The gate was still `OfflineReplayVerified` at session end** with the game
  and CE alive — the operator Find-what-writes window was handed off live.

## Session record

Session ID: `OD-RECOVERY-019`. Status: **Partial** (pipeline complete; root
step handed off live). Prior: OD-018 (Partial). BLK-0019 remains open — only
the single Churchill replay (sha12 `0FAE5612491E`) exists in the game folder;
`independentReplays` still 0.

### What was done

1. Pre-flight: stale CE (43432) and expired host (PID 41624) from OD-018 were
   present; both cleaned by the launch helper.
2. CE CLI Lua probe: `cheatengine-x86_64.exe -luac` and `--luac` with a
   marker-file test script both produced **no marker** — CE 7.7 does not
   auto-execute Lua from the command line. Confirms `prearm-attach.lua`
   (Ctrl+Alt+L) stays the operator-loaded staging path.
3. Detached launch helper (`nohup`) → `OK OfflineReplayVerified`.
4. Session driver (temp `scripts/od-018-session.ps1`, now parameterized):
   wait-verified → `pre-arm-debugger.ps1 -AutoAttach` (exit 0) → fixed rolling
   driver `-TargetSurvivors 10` with `-AddressFile` (exit 0).
5. Post-session query (13:05:46Z): gate still `OfflineReplayVerified`, game
   PID 42388 alive, CE PID 45188 attached — live handoff.

### CE CLI Lua probe (negative result, recorded)

`-luac` and `--luac` both rejected on CE 7.7 — no marker file written. This
rules out command-line Lua automation as a staging path and documents why the
existing `prearm-attach.lua` must be executed inside CE by the operator.

## Rules out / do not repeat

- **Ruled out:** CE 7.7 command-line Lua auto-execution (`-luac`/`--luac`) as a
  staging/automation path — probe wrote no marker file.
- **Do not repeat without a changed hypothesis:** a blocking foreground
  launch-helper invocation under a short wrapper timeout (the post-watch
  helper stall past 420s wasted OD-017 attempt-1 and OD-018 attempt-1; the
  detached launch + polling session driver is the proven pattern).

## Validation commands run

- `dotnet build WotBTreader.sln -c Release` — succeeded, 0 warnings/0 errors.
- Live gate queries — `OfflineReplayVerified` during and after the session.
- Address file line count: 7 (matches survivors=7).
- CE probe marker check — absent for both `-luac` and `--luac` (documented
  negative).

## Changed files this session

- `docs/operations/offset-discovery-ledger.md` — OD-019 index row + full YAML
  record; decision register `Next planned session` → OD-020; header updated.
- `docs/operations/offset-discovery-workflow.md` — next-session protocol →
  OD-020; CE CLI Lua negative probe note.
- `scripts/od-018-session.ps1` — **untracked** temp session driver, now
  parameterized (`-AddressFile`, `-TargetSurvivors`, `-TransitionSeconds`) for
  reuse.
- `scripts/od-query-state.ps1` — **untracked** temp gate query helper.
- `docs/operations/handoffs/2026-08-03-od-recovery-019.md` — this handoff.
- OD-018 recording (ledger/workflow/handoff from the prior unit) remains
  uncommitted in the working tree.

## Assumptions and unknowns

- The interactive Find-what-writes on the 7 staged survivors is the live
  outstanding step; the window was green at handoff but the 120s lease is the
  hard budget.
- The 7 survivors have not been exercised by Find-what-writes; address kind
  remains `heap-dynamic`/`private-mapping` pending the interactive root.
- CE PID 45188 attached to the live game at handoff; if it dies with the game,
  the next launch helper stops stale CE.
- BLK-0019 (content-distinct second replay) still open.

## Integration risks

- The interactive step must run while the gate is green — the operator should
  use the staged `%TEMP%\od-survivors-019.txt` (or load `prearm-attach.lua`)
  immediately post-verify in the next session.
- Detached launch + session driver remains the proven launch pattern; avoid a
  blocking foreground helper.

## Recommended next steps

1. Run `OD-RECOVERY-020`: detached launch → session driver → rolling to ≤10
   with `-AddressFile` → **operator interactive Find-what-writes immediately
   post-verify** on the staged survivors (CE pre-armed; lease margin
   reserved).
2. Commit the OD-018 + OD-019 recording (ledger + workflow + handoffs) when
   asked.
3. Consider committing the session driver as reusable tooling
   (`scripts/od-session-driver.ps1`) — it is now parameterized and proven.
4. Place a content-distinct second `.wotbreplay` in the game folder for
   BLK-0019 when available.
5. Push `03ff3a4`, `e9b2bd9`, `6be2f64`, `63845d7`, `880b6f3` (+ recording
   units) to origin/main when asked.
