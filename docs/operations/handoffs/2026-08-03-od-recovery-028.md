# Handoff: OD-RECOVERY-028 (extended operator window — lease-bound, not timer-bound)

**Date:** 2026-08-03
**Status:** Partial — rolling reached 9 survivors; the operator window held gate-green through ~90s+ (lease-bound)
**Campaign:** WoT Blitz PC offset discovery (`playerYaw` hypothesis quarantined; offset remains 0)

## Outcome

OD-027 showed the gate stays green past a 60s hold, so this session extended
the operator window from a fixed 60s timer to up to 240s — and since the
driver exits early on gate loss, the cap simply hands the operator the whole
remaining lease.

| Step | Result |
|---|---|
| Launch (detached) | Gate `OfflineReplayVerified` |
| Pre-arm | CE 7.7 attached (game 45376 / CE 27892) |
| Steady-state gate | Snapshot sane — 66,586,557 candidates (stable large baseline accepted at 100M threshold) |
| Rolling (5s transitions) | `789811→176224→30971→6304→570→99→22→9` — **9 survivors in 8 rounds** |
| Address file | Exactly 9 lines, no WARN (`%TEMP%\od-survivors.txt`) |
| Autorun staging | All 9 staged in CE address list as `od-survivor-1..9`; `debug_setBreakpoint` armed 4 (x64 HW limit), 0 hits |
| **Operator window (up to 240s)** | **Gate `OfflineReplayVerified` through 3 periodic re-announcements (~90s+ of green hold)** — the window is now lease-bound (closed by `EvidenceStale` on lease budget consumption, not by a timer) |

## Session record

- **Ledger:** OD-RECOVERY-028 index row + full YAML record; decision register → OD-RECOVERY-029; header updated.
- **Workflow doc:** next-session protocol → OD-RECOVERY-029 with the extended-hold note.
- **Handoff:** this file.

## What was done

1. Pre-flight: OD-027 residue (game 51336, CE 49548) present; Release build green (0 warnings/errors).
2. Extended the session driver's operator hold: `-HoldAfterRollSeconds` default 0→240 (the loop already exits early on gate loss, so this is a cap, not a hard timer), and added periodic re-announcement every 30s so the Find-what-writes instruction stays visible on the live transcript for the whole window.
3. Detached launch helper → session driver (`-TransitionSeconds 5 -HoldAfterRollSeconds 240`).
4. Gate `OfflineReplayVerified`; CE pre-armed; steady-state gate accepted the stable 66M baseline.
5. Rolling `789811→…→9` (8 rounds); address file exactly 9 lines, no WARN.
6. CE autorun staged all 9 survivors; armed 4 write-BPs; 0 hits (consistent with the OD-020 rules-out).
7. Operator window held in-command: gate `OfflineReplayVerified` through 3 re-announcements (~90s+) then `EvidenceStale` on lease budget consumption.
8. Recorded per repo rules (ledger + workflow + this handoff).

## Rules out / do not repeat

- **Do not repeat:** automated CE Windows-debugger write-BP capture as a
  replacement for interactive Find-what-writes (OD-020 rules-out; zero hits
  again this session). The interactive right-click remains the evidence path.
- Steady-state gate calibration rule stands (OD-026): accept the measured
  stable baseline, don't assume one.

## Assumptions and unknowns

- No operator Find-what-writes result recorded by session end (operator-owned;
  the window was held lease-bound inside the command).
- Same Churchill replay sha12 `0FAE5612491E`; `independentReplays` still 0
  (BLK-0019 open).
- No RIP/root-address evidence recorded yet; the offset remains 0.

## Integration risks / operational notes

- The operator window is now bounded by the 120s research lease, not the hold
  timer: with `-HoldAfterRollSeconds 240` the driver polls the gate and holds
  until it goes stale. If a longer lease is ever configured, the cap can rise
  with it.
- Stale CE / host processes are stopped automatically by the next launch helper.

## Validation commands

```powershell
dotnet build WotBTreader.sln -c Release   # ran this session: 0 warnings / 0 errors
powershell -NoProfile -ExecutionPolicy Bypass -Command '$t=$null;$e=$null; [System.Management.Automation.Language.Parser]::ParseFile("scripts\od-018-session.ps1",[ref]$t,[ref]$e)|Out-Null; if($e.Count -eq 0){"DRIVER_PARSE_OK"}else{$e|%{$_.Message}}'   # ran this session: DRIVER_PARSE_OK
./scripts/validate.ps1   # full gate — run before milestone commits (not run this session)
```

## Changed files this session

- `docs/operations/offset-discovery-ledger.md`
- `docs/operations/offset-discovery-workflow.md`
- `docs/operations/handoffs/2026-08-03-od-recovery-028.md`
- `scripts/od-018-session.ps1` (temp helper; operator hold default 0→240s + periodic re-announce)

## Recommended next steps

1. **OD-RECOVERY-029 (120s research-lease timebox):** run the same pipeline
   (steady-state gate + 5s transitions + `-HoldAfterRollSeconds 240`) with the
   operator present; during the held green window, right-click any
   `od-survivor-N` entry in CE → "Find out what writes this address" and let
   the replay play. Record the write instruction + register (member
   displacement like `[reg+0x34]`).
2. Import a content-distinct second `.wotbreplay` to close BLK-0019
   (`independentReplays`).
