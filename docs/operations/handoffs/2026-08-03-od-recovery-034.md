# Handoff — OD-RECOVERY-034 (2026-08-03)

## Sessions worked

- `OD-RECOVERY-034` (1 attempt; see ledger) — **Partial**

## Result statuses

- **OD-RECOVERY-034: Partial.** The changed hypothesis (OD-034's mandate) was
  the **two-phase tail transition shave** (1s pulse below 200 survivors). It
  was implemented in the rolling driver (`TailThreshold=200`,
  `TailTransitionSeconds=1` params, passthrough in the session driver),
  parse-checked, and validated live: **18 rounds fit the 120s lease** (vs
  14–15 before), with the 401 refresh firing live an eighth time (round 10).
  But the survivor count **plateaued at 12 through four consecutive 1s-pulse
  rounds** (sequence `906685→…→48→16→13→12→12→12→12`) — the last survivors
  tick every frame and survive even 1s pulses. **The tail is value-bound,
  not round-bound**: rolling alone cannot disambiguate the high-frequency
  tickers, so the interactive Find-what-writes step is required regardless
  of lease headroom. No ≤10 target, no address file, no operator window.
  Offset remains 0; nothing promoted.

## Successes

1. **Two-phase tail shave implemented + validated live** — a fourth durable
   driver optimization (`TailThreshold` / `TailTransitionSeconds` params with
   session-driver passthrough), giving 4 extra rounds in the lease.
2. **401 refresh live an eighth time** (round 10) — long rolls keep surviving
   token rotation.
3. **Conclusive plateau diagnosis**: four consecutive 1s-pulse rounds at 12
   survivors proves the tail survivors increase every frame — pulse shaving
   cannot shed them.

## Failures / open items

- **The tail is value-bound**: no amount of short-pulse rounds reaches ≤10;
   the interactive step (or a different field pivot) is required.
- No address file, no CE staging, no operator window (target not reached).
- **BLK-0019**: still needs a content-distinct second replay
  (`independentReplays` 0).

## Explicitly ruled out

- **More lease-headroom by pulse shaving alone will not reach ≤10** (OD-034):
  the tail survivors tick every frame and survive 1s pulses.

## Must not repeat without a changed hypothesis

- Repeating pulse-shave-only runs to chase ≤10 (OD-034: plateau at 12 across
  four 1s rounds). The changed-hypothesis space for OD-035 is the interactive
  step (operator presence at a green window with a staged ~12-survivor set),
  a snapshot byte budget / region-selection passthrough to open the operator
  window with lease to spare, or a content-distinct second replay.

## Next session ID and timebox

- **OD-RECOVERY-035**: rolling alone cannot finish the job. Priority: (a)
  operator presence at a green window for interactive Find-what-writes on a
  staged ~12-survivor set, (b) snapshot budget passthrough for a live
  operator window, or (c) content-distinct second replay for BLK-0019.

## Validation commands and results

- `scripts/roll-replay-time-increased.ps1` + `scripts/od-018-session.ps1`:
  parse-check `ROLL_PARSE_OK` / `SESS_PARSE_OK`.
- Live gate state at session end: `EvidenceStale` / `evidence.expired`
  (lease expiry, expected teardown state).
- CE autorun polled 300s; no address file appeared (target not reached —
  correct behavior).
- Process teardown verified clean (no wotblitz / cheatengine / Host.Web /
  dotnet leftovers).

## Unrelated local/untracked material left untouched

- `docs/operations/handoffs/2026-08-02-od-recovery-014-partial.md` remains a
  line-ending-only artifact (blob matches index; no real change).
