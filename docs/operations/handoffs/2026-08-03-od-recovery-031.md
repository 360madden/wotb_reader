# Handoff — OD-RECOVERY-031 (2026-08-03)

## Sessions worked

- `OD-RECOVERY-031` (5 attempts; see ledger) — **Partial**
- Also: docs propagation + commit/push for the OD-030 session records
  (`2026-08-03-docs-propagation-commit.md`), committed as `44028ba`.

## Result statuses

- **OD-RECOVERY-031: Partial.** First target convergence since OD-020: rolling
  reached **10 ≤ 10 survivors on attempts 4 and 5**. Candidate-count
  optimization (1 candidate per rolling round, harvest full set only on the
  target round) validated live: **10–14 rounds fit the 120s lease** vs 6–7
  before. CE staging handoff defect root-caused and fixed (default
  address-file path + 300s autorun poll): attempt 5 staged **all 10 survivors
  in CE's address list with 4 HW write-BPs armed**. The operator window still
  opened with the gate `EvidenceStale` both target runs — the lease expires
  exactly as the 66M-baseline roll ends. Offset remains 0; nothing promoted.

## Successes

1. **Target convergence solved**: rolling reached ≤10 inside the lease twice
   (attempt 4: 66.4M→10 in 10 rounds; attempt 5: 66.3M→10 in 14 rounds,
   sequence `983469→190633→36821→7563→1346→572→72→30→15→14→13→13→12→10`).
2. **Candidate-count harvest optimization** live-validated (attempts 3–5);
   doubled rounds-in-lease, enabled target convergence.
3. **401 refresh** fired live a fourth/fifth time (attempt 3 round 8, attempt
   5 round 10) — long rolls survive token rotation.
4. **CE staging handoff fixed**: root cause was a custom `-AddressFile` name
   bypassing the autorun's default-path poll plus a 90s poll window shorter
   than the roll; fixed with default path + 300s poll. Attempt 5: `loaded 10
   survivor lines → staged 10 in address list → armed 4 write-BPs`.
5. **Game-side flake re-confirmed** (attempt 2: launcher-green →
   `EvidenceStale`/`evidence.expired`, no blitz-log written) — relaunch, not
   re-click.

## Failures / open items

- **Operator window stale both target runs** (attempts 4/5): gate
  `EvidenceStale` at window open — the 120s lease expires exactly at roll
  end. Rolling + staging are solved; the live interactive Find-what-writes
  window is the only missing piece.
- **0 breakpoint hits** on the automated write-BP path (consistent with the
  OD-020 standing rule — interactive step remains the evidence path).
- **BLK-0019**: still needs a content-distinct second replay
  (`independentReplays` 0).

## Explicitly ruled out

- No new rules-outs this session. Automated CE Windows-debugger write-BPs on
  rolling Double survivors remain ruled out (OD-020; 0 hits again).

## Must not repeat without a changed hypothesis

- Custom `-AddressFile` name that bypasses the CE autorun's default-path poll
  (OD-031 attempt 4) — use `%TEMP%\od-survivors.txt`.
- 90s CE autorun poll window for a 66M-baseline roll — keep 300s.
- `maxCandidates=500` on every rolling round — request 1, harvest on target
  only.

## Next session ID and timebox

- **OD-RECOVERY-032**: same pipeline (driver at best-known state). Rolling
  now fits the lease, so target the lease headroom for the operator window:
  start the interactive step immediately at roll end, and consider reducing
  round-1 walk cost (snapshot budget / region selection) or importing a
  content-distinct second replay for BLK-0019.

## Validation commands and results

- `scripts/roll-replay-time-increased.ps1`: parse-check `PARSE_OK`.
- `tools/cheat-engine/od-autorun-writebp.lua`: updated poll window (45 → 150
  rounds = 300s), reinstalled to CE autorun folder, diff IDENTICAL.
- Live gate state at session end: `EvidenceStale` / `evidence.expired`
  (lease expiry, expected teardown state).

## Unrelated local/untracked material left untouched

- `docs/operations/handoffs/2026-08-02-od-recovery-014-partial.md` remains a
  line-ending-only artifact (blob matches index; no real change).
- CE / web host processes stopped at session end by the next launch helper or
  explicit teardown.
