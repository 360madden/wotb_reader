# Handoff: OD-RECOVERY-027 (steady-state gate held; 7 survivors; window green at command end)

**Date:** 2026-08-03
**Status:** Partial — rolling reached 7 survivors with the gate green through the entire operator window; interactive step still operator-owned
**Campaign:** WoT Blitz PC offset discovery (`playerYaw` hypothesis quarantined; offset remains 0)

## Outcome

The pipeline with the steady-state gate reproduced cleanly, and the operator
window reached its best end state yet.

| Step | Result |
|---|---|
| Launch (detached) | Gate `OfflineReplayVerified` |
| Pre-arm | CE 7.7 attached (game 51336 / CE 49548) |
| Steady-state gate | Snapshot sane — 66,571,431 candidates (stable large baseline accepted at 100M threshold) |
| Rolling (5s transitions) | `915639→208523→38566→6357→802→220→139→7` — **7 survivors in 8 rounds** (tied with OD-019; campaign record remains OD-020's 5) |
| Address file | Exactly 7 lines, no WARN (`%TEMP%\od-survivors.txt`) |
| Autorun staging | All 7 staged in CE address list as `od-survivor-1..7`; `debug_setBreakpoint` armed 4 (x64 HW limit), 0 hits |
| **Operator window (60s)** | **Gate `OfflineReplayVerified` at window open AND at the final read after the hold — the interactive window was live and green at command end** (EvidenceStale only after recording began) |

## Session record

- **Ledger:** OD-RECOVERY-027 index row + full YAML record; decision register → OD-RECOVERY-028; header updated.
- **Workflow doc:** next-session protocol → OD-RECOVERY-028 with the OD-027 summary.
- **Handoff:** this file.

## What was done

1. Pre-flight: OD-026 residue (game 48404, CE 42468) present; Release build green (0 warnings/errors).
2. Detached launch helper → session driver (`-TransitionSeconds 5 -HoldAfterRollSeconds 60`).
3. Gate `OfflineReplayVerified`; CE pre-armed; steady-state gate accepted the stable 66M baseline.
4. Rolling `915639→…→7` (8 rounds); address file exactly 7 lines, no WARN.
5. CE autorun staged all 7 survivors; armed 4 write-BPs; 0 hits (consistent with the OD-020 rules-out).
6. Operator window held 60s in-command; gate still `OfflineReplayVerified` at the final read after the hold — the window was live and green at command end.
7. Recorded per repo rules (ledger + workflow + this handoff).

## Rules out / do not repeat

- **Do not repeat:** automated CE Windows-debugger write-BP capture as a
  replacement for interactive Find-what-writes (OD-020 rules-out; zero hits
  again this session). The interactive right-click remains the evidence path.
- Steady-state gate calibration rule stands (OD-026): accept the measured
  stable baseline, don't assume one.

## Assumptions and unknowns

- No operator Find-what-writes result recorded by session end (operator-owned;
  window held live inside the command and green at the end).
- Same Churchill replay sha12 `0FAE5612491E`; `independentReplays` still 0
  (BLK-0019 open).
- No RIP/root-address evidence recorded yet; the offset remains 0.

## Integration risks / operational notes

- The gate stayed green through the hold this time, so the operator's window
  is the single remaining step; the driver holds it for 60s in-command.
- Stale CE / host processes are stopped automatically by the next launch helper.

## Validation commands

```powershell
dotnet build WotBTreader.sln -c Release   # ran this session: 0 warnings / 0 errors
powershell -NoProfile -ExecutionPolicy Bypass -Command '$t=$null;$e=$null; [System.Management.Automation.Language.Parser]::ParseFile("scripts\roll-replay-time-increased.ps1",[ref]$t,[ref]$e)|Out-Null; if($e.Count -eq 0){"ROLL_PARSE_OK"}else{$e|%{$_.Message}}'   # prior session: ROLL_PARSE_OK
./scripts/validate.ps1   # full gate — run before milestone commits (not run this session)
```

## Changed files this session

- `docs/operations/offset-discovery-ledger.md`
- `docs/operations/offset-discovery-workflow.md`
- `docs/operations/handoffs/2026-08-03-od-recovery-027.md`

## Recommended next steps

1. **OD-RECOVERY-028 (120s research-lease timebox):** run the same pipeline
   (steady-state gate + 5s transitions + 60s hold) with the operator present;
   during the held green window, right-click any `od-survivor-N` entry in CE →
   "Find out what writes this address" and let the replay play. Record the
   write instruction + register (member displacement like `[reg+0x34]`).
2. Import a content-distinct second `.wotbreplay` to close BLK-0019
   (`independentReplays`).
