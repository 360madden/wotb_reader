# Handoff — OD-RECOVERY-035 (2026-08-03)

## Sessions worked

- `OD-RECOVERY-035` (2 attempts; see ledger) — **Partial**

## Result statuses

- **OD-RECOVERY-035: Partial.** The changed hypothesis was the **snapshot
  byte-budget passthrough** (`OffsetSnapshotRequest MaxBytes`), implemented
  as `-SnapshotMaxBytes` in the rolling driver + a `-MaxRounds` passthrough
  in the session driver (default 0 = engine ceiling, unchanged). Validated
  live on both attempts:
  - Attempt 1 (`-SnapshotMaxBytes 402653184`): round-1 `previous=823,484`
    vs ~66M unbounded — **80× walk reduction**; rolled `823484→…→12` in 22
    rounds with **`rolling_exit=0` — full round budget completed inside the
    lease, the first non-lease-wall exit since OD-031**; 401 refresh live
    round 15; plateaued at 12 from round 15.
  - Attempt 2 (`-MaxRounds 40`): round-1 `previous=50,061,014` (budget bound
    ~50M); rolled `750293→…→17` in 16 rounds; plateaued 17 rounds 16–20;
    lease wall round 21 (`400` + `EvidenceStale`); 401 refresh live round 10.
  No ≤10 target staged; offset remains 0; nothing promoted.

## Successes

1. **Snapshot byte-budget passthrough implemented + validated live** — a
   fifth durable driver optimization: round-1 now walks 823K/50M instead of
   66M, and a full round budget completes inside the 120s lease.
2. **`rolling_exit=0` on attempt 1** — the first non-lease-wall roll exit
   since OD-031; rounds are now cheap.
3. **401 refresh fired live a ninth and tenth time** (rounds 15 and 10).
4. **Lease headroom is now decoupled from the plateau**: the value-bound
   tail (12/17) persists whether the lease is the wall (OD-032/033/034) or
   not (OD-035) — rolling alone cannot reach ≤10.

## Failures / open items

- **No ≤10 staging**: the every-frame tickers survive short pulses regardless
  of lease headroom, so the interactive Find-what-writes step is the only
  disambiguator.
- No address file (target not reached), no CE staging, no green operator
  window.
- **BLK-0019**: still needs a content-distinct second replay
  (`independentReplays` 0).

## Explicitly ruled out

- **Lease headroom alone cannot reach ≤10** — proven across lease-constrained
  (OD-032/033/034) and lease-viable (OD-035) rolls alike.

## Must not repeat without a changed hypothesis

- Repeating rolling-only runs to chase ≤10 without operator presence or a
  content-distinct replay (OD-032→OD-035: 8 attempts, plateau 11–17, no ≤10
  staging). The operator window can now open green — use it.

## Next session ID and timebox

- **OD-RECOVERY-036**: operator presence at a held green window for
  interactive Find-what-writes on a staged ~12-survivor set, with
  `-SnapshotMaxBytes 402653184 -MaxRounds 40 -HoldAfterRollSeconds 240`;
  alternatively a content-distinct second replay for BLK-0019.

## Validation commands and results

- `scripts/roll-replay-time-increased.ps1` + `scripts/od-018-session.ps1`:
  parse-check `ROLL_PARSE_OK` / `SESS_PARSE_OK` (after both edits).
- Live gate state at session end: `EvidenceStale` / `evidence.expired`
  (lease expiry, expected teardown state).
- CE autorun polled 300s both attempts; no address file appeared (target not
  reached — correct behavior).
- Process teardown verified clean (no wotblitz / cheatengine / Host.Web /
  dotnet leftovers).

## Unrelated local/untracked material left untouched

- `docs/operations/handoffs/2026-08-02-od-recovery-014-partial.md` remains a
  line-ending-only artifact (blob matches index; no real change).
