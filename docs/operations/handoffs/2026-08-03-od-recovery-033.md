# Handoff — OD-RECOVERY-033 (2026-08-03)

## Sessions worked

- `OD-RECOVERY-033` (2 attempts; see ledger) — **Partial**

## Result statuses

- **OD-RECOVERY-033: Partial.** Two attempts, both ran the full roll to the
  120s lease edge without reaching the ≤10 target: attempt 1 converged to
  **12 survivors in 14 rounds** (sequence `863861→…→12`), attempt 2 to
  **17 survivors in 13 rounds** (`938727→…→17`). The 401 refresh fired live a
  seventh time (rounds 9/10). No address file staged, no operator window.
  Offset remains 0; nothing promoted.

## Successes

1. **Pipeline reproduces cleanly** — no game-side crash, no gate anomaly
   either attempt; both launches ran the complete roll to the lease edge.
2. **401 refresh live a seventh time** (attempt 1 round 10, attempt 2 round
   9) — long rolls keep surviving token rotation.
3. **Convergence range now characterized**: 11–17 survivors across OD-032/033
   (4 attempts) under current load — every run lands just past the 120s lease
   edge, never ≤10 with staging.

## Failures / open items

- **The lease edge is the standing blocker**: convergence lands at 11–17 and
  the lease expires during the round right after the plateau (rounds 15 and
  14). More identical runs are not the path — OD-034 must change the
  hypothesis.
- No address file, no CE staging, no operator window (target not reached).
- **BLK-0019**: still needs a content-distinct second replay
  (`independentReplays` 0).

## Explicitly ruled out

- No new rules-outs. Automated CE write-BPs on rolling Double survivors stay
  ruled out (OD-020); the interactive operator step remains the evidence
  path.

## Must not repeat without a changed hypothesis

- More identical best-known-driver runs to chase ≤10 (OD-032/033: 4 attempts,
  11–17 survivors, all past the lease edge). The changed hypothesis for
  OD-034 is lease headroom: reduce round-1 walk cost (snapshot byte budget /
  region selection passthrough via MemoryScanEngine `MaxBytes`), shave the
  tail transition, or run in a lighter-loaded window.

## Next session ID and timebox

- **OD-RECOVERY-034**: change the hypothesis, not the parameters. Priority:
  snapshot byte budget / region selection passthrough to cut the round-1 66M
  walk, or a tail-transition shave; a content-distinct second replay still
  helps BLK-0019.

## Validation commands and results

- Live gate state at session end: `EvidenceStale` / `evidence.expired`
  (lease expiry, expected teardown state).
- CE autorun polled 300s both attempts; no address file appeared (target not
  reached — correct behavior).
- Process teardown verified clean (no wotblitz / cheatengine / Host.Web /
  dotnet leftovers).

## Unrelated local/untracked material left untouched

- `docs/operations/handoffs/2026-08-02-od-recovery-014-partial.md` remains a
  line-ending-only artifact (blob matches index; no real change).
