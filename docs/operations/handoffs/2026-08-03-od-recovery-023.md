# Session handoff — 2026-08-03: OD-RECOVERY-023 (operator Find-what-writes window delivered live)

**Author:** Codex Agent

**Branch:** `main` (head `880b6f3`; working tree: OD-018/019/020 recording
still uncommitted, plus OD-023 recording, untracked temp helpers
`scripts/od-query-state.ps1` / `scripts/od-018-session.ps1`, and
`tools/cheat-engine/od-autorun-writebp.lua`)

## Outcome

`OD-RECOVERY-023` ran as **Partial** — the proven pipeline completed
end-to-end (sixth consecutive green run) and delivered the operator
Find-what-writes window **live** with survivors staged in CE:

- Gate `OfflineReplayVerified` (query 13:46:01Z).
- CE 7.7 pre-armed and attached to `wotblitz.exe` (PID 43460 → CE PID 41312).
- Rolling `765363→181456→31840→4266→596→89→22→16→16→14→11→9` (9 survivors).
- **CE autorun staged all 9 survivors into CE's address list**
  (`od-survivor-1..9`) — operator Find-what-writes is a single right-click.
- Address file `%TEMP%\od-survivors.txt` = 9 lines, count == survivors, no WARN.

The operator-owned interactive Find-what-writes window was green at handoff —
the first time in the campaign that survivors were staged in CE while the gate
held.

## Session record

Session ID: `OD-RECOVERY-023`. Status: **Partial**. Prior: OD-020 (Partial).
BLK-0019 remains open — only the single Churchill replay (sha12
`0FAE5612491E`) exists in the game folder; `independentReplays` still 0.

### What was done

1. Pre-flight: no stale game/CE; stale host (PID 42928) on 9182 stopped by the
   launch helper.
2. Release build: 0 warnings / 0 errors.
3. Detached launch helper → `OfflineReplayVerified`.
4. Session driver: pre-arm (`pre-arm-debugger.ps1 -AutoAttach`, exit 0) →
   fixed rolling driver (`-TargetSurvivors 10`), exit 0.
5. CE autorun (`od-autorun-writebp.lua`) attached, staged 9 survivors,
   resolved `debug_setBreakpoint`, armed 4 (HW limit), 0 hits (consistent with
   the OD-020 rules-out; interactive step is the evidence path).
6. Handoff query (13:46:01Z): gate still `OfflineReplayVerified`, game alive,
   CE attached with 9 staged survivors.

## Rules out / do not repeat

- **Ruled out (no new beyond OD-020):** automated CE Windows-debugger
  write-BP hit capture; CE CLI Lua auto-execution (`-luac`/`--luac`).
- **Do not repeat without a changed hypothesis:** further probing of the
  automated CE write-BP capture path — the operator interactive step is the
  evidence path.

## Validation commands run

- `dotnet build WotBTreader.sln -c Release` — 0 warnings / 0 errors.
- Live gate query at handoff — `OfflineReplayVerified` (13:46:01Z).
- Autorun log review — staged 9, armed 4, hits 0.
- Address file count — 9 lines.

## Changed files this session

- `docs/operations/offset-discovery-ledger.md` — OD-023 index row + YAML
  record; decision register → OD-024; header updated.
- `docs/operations/offset-discovery-workflow.md` — next-session protocol →
  OD-024; autorun staging note.
- `docs/operations/handoffs/2026-08-03-od-recovery-023.md` — this handoff.
- Prior OD-018/019/020 recording remains uncommitted in the working tree.

## Assumptions and unknowns

- Whether the operator completed Find-what-writes on the 9 staged survivors is
  unknown at write time (operator-owned step; window delivered live).
- The 9 survivors have not yielded instruction/register evidence yet; address
  kind remains `heap-dynamic`/`private-mapping` pending the interactive root.
- BLK-0019 (content-distinct second replay) still open.

## Integration risks

- The green window is the hard constraint: next session must either reuse the
  live handoff pattern or have the operator ready at the CE window the moment
  rolling lands.
- Detached launch + session driver remains the proven launch pattern.

## Recommended next steps

1. `OD-RECOVERY-024` (120s research-lease timebox): operator runs interactive
   Find-what-writes on the staged survivors during the live green window
   (CE pre-armed; `od-survivor-1..9` in the address list), or reruns the
   pipeline with the operator ready at the CE window.
2. Record any captured write instruction + register as the structural
   hypothesis (member displacement) in the ledger.
3. Commit the OD-018/019/020/023 recordings + tooling when asked.
4. Place a content-distinct second `.wotbreplay` in the game folder for
   BLK-0019 when available.
5. Push `03ff3a4`, `e9b2bd9`, `6be2f64`, `63845d7`, `880b6f3` (+ recording
   units) to origin/main when asked.
